setup:
  addons:
    - plan: heroku-postgresql
      as: cxp_db

build:
  docker:
    web: Dockerfile

release:
  image: web
  command:
    - export FLASK_APP='src.api:create_api()'
    - flask db init
    - flask db migrate
    - flask db upgrade

run:
  web: gunicorn 'wsgi:create_wsgi()'
