setup:
    addons:
        - plan: heroku-postgresql
          as: db
build:
    docker:
        web: Dockerfile
release:
    image: web
    command:
        - flask db init
        - flask db migrate
        - flask db upgrade
run:
    web: gunicorn 'wsgi:create_wsgi()' --log-file -
