setup:
  addons:
    - plan: heroku-postgresql
      as: cxp_db

build:
  docker:
    web: Dockerfile

release:
  image: web
  command:
    - flask db upgrade

run:
  web: gunicorn 'wsgi:create_wsgi()'
